import { AbstractList } from "@/components/abstract-list";
import React from "react";

const Abstract = () => {
  return (
    <div>
      <AbstractList />
    </div>
  );
};

export default Abstract;
